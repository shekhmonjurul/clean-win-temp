# 🛠 Developer Documentation - Temp Cleaner Script

এটি একটি পাইথন ভিত্তিক অটোমেশন স্ক্রিপ্ট যা `os`, `shutil`, এবং `ctypes` মডিউল ব্যবহার করে উইন্ডোজ ফাইল সিস্টেম ম্যানেজ করে।

## 🏗 টেকনিক্যাল আর্কিটেকচার

### ব্যবহৃত মডিউলসমূহ:
*   `os`: এনভায়রনমেন্ট ভ্যারিয়েবল থেকে পাথ খুঁজে বের করা এবং ফাইল আনলিঙ্ক করার জন্য।
*   `shutil`: সাব-ডিরেক্টরি এবং তার কন্টেন্ট রিকাসিভলি রিমুভ করার জন্য (`shutil.rmtree`)।
*   `ctypes`: উইন্ডোজের `shell32.dll` ব্যবহার করে অ্যাডমিন প্রিভিলেজ চেক এবং রিকোয়েস্ট করার জন্য।
*   `sys`: ইন্টারপ্রেটার পাথ এবং আর্গুমেন্ট পাস করার জন্য।

### প্রধান ফাংশনসমূহ:
1.  **`is_admin()`**: 
    `ctypes.windll.shell32.IsUserAnAdmin()` কল করে চেক করে স্ক্রিপ্টটি উচ্চতর পারমিশনে চলছে কি না।
    
2.  **`run_as_admin()`**: 
    যদি ইউজার অ্যাডমিন না হয়, তবে এটি `ShellExecuteW` মেথড ব্যবহার করে `runas` প্যারামিটার দিয়ে পুনরায় স্ক্রিপ্টটি রান করে।
    
3.  **`clean_folders()`**: 
    মূল লজিক। এটি একটি ট্রাই-এক্সেপ্ট (`try-except`) ব্লকের ভেতর কাজ করে যাতে "File in use" এরর আসলে স্ক্রিপ্টটি ক্র্যাশ না করে।

## 🛠 মডিফিকেশন গাইড
আপনি যদি আরও নতুন কোনো পাথ (যেমন: Chrome Cache) যোগ করতে চান, তবে `folders_to_clean` লিস্টে সেই পাথটি স্ট্রিং হিসেবে যোগ করে দিন:
```python
folders_to_clean = [
    # ... আগের পাথসমূহ
    r'C:\Custom\Path\To\Clean'
]